# Resolve the actual devices on the Raspberry Pi before changing these values.
ARMPI_SERIAL_DEVICE=/dev/ttyAMA0
ARMPI_CAMERA_DEVICE=/dev/video0

# Use the same domain ID on any computer that needs to discover this robot.
ROS_DOMAIN_ID=0
